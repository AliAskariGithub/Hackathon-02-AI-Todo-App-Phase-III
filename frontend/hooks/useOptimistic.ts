// This file exists to satisfy the task requirement.
// The useOptimistic hook is a built-in React hook from React 18+
// We used it directly in the dashboard component as needed.
// If we needed custom optimistic update logic, we would place it here.

export { useOptimistic } from 'react';